#include <stdio.h>
#include <stdint.h>

void foobar() {
}

void readhex_hex_read() {
  size_t length_str = 0;
  char target[1];

  char next;
  target[0] = next - '0';
}

int main(int argc, char** argv) {
  char retval = 0;

  for(size_t i = 1; i < argc; ++i) {
     if(!getc(stdin))
      foobar();
  }

  readhex_hex_read();

  return 0;
}
